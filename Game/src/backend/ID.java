package backend;

/**
 *
 * @author David
 */
public enum ID {
    Player(), Enemy(), StationaryObject(), MovingObject(), Character(), HealthMeter(), EnemyProjectile(), FriendlyProjectile(), NeutralProjectile(), Background(), Tile();
}
